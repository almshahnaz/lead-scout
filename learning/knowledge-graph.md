# Lead Scout — knowledge graph

The living map of what I actually know. Updated after every lesson. This file — not gut feel — decides what I get quizzed on.

## How this file works

- **Status ladder:** `seed` (not yet taught) → `introduced` (explained once) → `practicing` (used it, with help) → `understood` (explained in my own words *and* passed a check).
- Statuses only ever move up on evidence of something I actually said or did — never on assumption, and never because a topic "seems basic."
- Don't re-quiz anything currently `understood` with a recent last-reviewed date. Prioritize quizzing on `introduced` and `practicing` entries, oldest last-reviewed first.
- Every entry needs a one-line evidence field: what was actually said/done that justifies the status.
- Prior general experience (the CS degree, known languages) is background context, not evidence — status here reflects what's been demonstrated *in this project's tracked loop*, not what I already knew coming in. It'll move fast once touched, but starts honest.

---

## Low-level (variables, loops, functions)

| Concept | Status | Introduced | Last reviewed | Evidence |
|---|---|---|---|---|
| Variables | seed | — | — | Not yet taught in-project. |
| Functions | seed | — | — | Not yet taught in-project. |
| Loops | seed | — | — | Not yet taught in-project. |
| Conditionals (if/else) | practicing | 2026-09-05 | 2026-09-05 | Task 4.3: first real use of a plain `if`/`else` statement in this project (previously only `&&` and `? :` expressions). After being shown the shape and asked to branch the response on whether `match` existed, wrote the full `if (match) { res.json(match); } else { res.status(404).json({ error: "not found" }); }` correctly on the first attempt. |
| Data types (objects, arrays, primitives) | understood | 2026-08-26 | 2026-09-03 | Task 2.1: user wrote two object literals (an object with string/enum fields) as entries in a `CompanyResult[]` array, matching a given interface — used with a worked example as a guide. Task 3.2 review question: unprompted, correctly identified `{ status: "ok" }` as an object and distinguished it from a primitive string by noting an object "can be destructured" — a correct, independent free-recall explanation. |
| Async/await & Promises | practicing | 2026-09-03 | 2026-09-05 | Task 3.5: after the buzzer/receipt analogy, wrote a correct `async function checkHealth()` with two chained `await`s (`fetch()` then `response.json()`) unassisted, plus the "define an async helper inside the effect" pattern needed since `useEffect`'s own callback can't be `async`. Task 4.5: extended to a harder shape — an `async handleSubmit` chaining two dependent `fetch` calls, where the second call's URL is built from data (`result.id`) returned by the first. Wrote the overall structure correctly after describing the plan unprompted; needed two corrections along the way (tracked separately: the `POST` body shape, and a copy-pasted `response.status` instead of `fetchBatch.status` in the second error branch), both self-fixed once pointed out. Task 5.3: correctly described the plan unprompted (`async` function, `await pool.query(...)`, log success/failure) before writing code, and wrote it correctly on the first attempt. Real gap surfaced and self-corrected: first version paired the `try`/`catch` with a redundant `if (response) {...} else {...}` (dead code, since `pool.query` resolves to a real, always-truthy result on success and only ever fails by *rejecting* — never resolving falsy); after being asked to reason through which branch would actually run on a real failure, correctly answered "the catch block," then independently simplified the code to remove the unreachable `else`. Task 5.5: the exact same shape recurred (`if (result) { res.json(result) } else { 404 }`) in a first attempt at `GET /batches/:id` — needed a direct callback to yesterday's fix ("is `result` itself ever going to be falsy here?") rather than catching it unprompted this time, then correctly answered "I guess it would never be falsey" and moved to checking `result.rows.length` instead. Recurrence noted honestly — the underlying truthy/falsy mechanism is understood when prompted, but hasn't yet prevented the mistake from being written in the first place. |
| Fetch with a `POST` body (`method`, `headers`, `JSON.stringify`) | practicing | 2026-09-05 | 2026-09-05 | Task 4.5: correctly described the plan unprompted (method, headers, body) before writing code, drawing the connection to the backend's `express.json()` middleware unassisted. First written attempt sent `JSON.stringify(companyNames)` — the bare array — instead of wrapping it in an object, which didn't match the backend's `const { companyNames } = req.body` destructuring. Traced through the mismatch via questions (what `req.body` would actually contain, what destructuring a missing property yields) rather than being told directly, then self-corrected to `JSON.stringify({ companyNames })` using the already-known object-shorthand pattern. |
| `try`/`catch` error handling around `fetch` | practicing | 2026-09-05 | 2026-09-05 | Task 4.5: added `try`/`catch` and a `response.ok` check around both `fetch` calls entirely unprompted — not yet formally taught in this project, but used correctly (throwing on a non-ok response, catching and logging in one place) on the first attempt for both requests. |
| JS `undefined` as a real value (vs. "nothing happens") | practicing | 2026-09-05 | 2026-09-05 | Recurring mispredict, first seen in task 4.2 review (an unreturned `.map()` callback predicted to produce "nothing"; actually an array of `undefined`s) and again in task 4.5: asked what `const { companyNames } = arr` prints when `arr` is a plain array with no such property, predicted "it would print nothing" — corrected that a missing property destructures to the real value `undefined`, and `console.log(undefined)` prints the literal word `undefined`. Two independent instances of the same misconception; flagged as its own tracked concept rather than folded into destructuring or return-statement entries. |
| TypeScript static typing | understood | 2026-08-23 | 2026-09-07 | Task 2.1: user filled in two `CompanyResult` object literals matching a given interface, then correctly explained in their own words why `status: "done" \| "pending" \| "failed"` (a union type) is stricter than `string` — "it only allows those 3 statuses, anything else would result in an error." Task 2.5: hit a real `setResults` type error caused by string-literal widening (an untyped `notFound` object's `status: "pending"` widened to `string`, not the literal); when pointed back to how `fakeResults.ts` typed its object literals, independently applied the same `notFound: CompanyResult = {...}` annotation and it resolved the error — a genuine transfer of the technique to a new, subtler case. Weekly review (2026-09-07, forward-looking): asked why `pool.query()`'s generic return type would matter for the upcoming Postgres swap; correctly answered that without annotating the result to the real shape, TypeScript would fall back to comparing against a generic `string` instead of the actual union type — directly reapplying the 2.1 example, unprompted. |
| `Promise.all()` (array of Promises → one Promise resolving to an array of real values) | practicing | 2026-09-07 | 2026-09-07 | Task 5.4: added `async` to a `.map()` callback (to `await` an insert inside it), which made `results` an array of Promises rather than `CompanyResult[]`. First prediction of the resulting type was incomplete ("it will return a promise"); after a follow-up asking what `.map()` itself always returns, correctly answered "an array of promises." Then correctly applied `await Promise.all(companyNames.map(async (name) => {...}))` to resolve it back to the real array, and gave an accurate unprompted summary of why afterward ("map function always returns an array... so we use Promise.all to turn the array of promises into a single promise that resolves into an array of the real values"). |
| Type-level vs. value-level declaration order (interfaces resolved file-wide; `const`/`let` are not) | practicing | 2026-09-04 | 2026-09-04 | Task 4.1: declared `const batchRuns: BatchRun[]` above the `BatchRun` interface itself, then correctly predicted this would compile fine — confirmed via `npx tsc --noEmit` (exit 0). First real contact with the type/value distinction in declaration order, capped at `practicing`. |
| Array methods for data transformation (`.filter()`, `.find()`, chaining) | practicing | 2026-08-30 | 2026-09-05 | Task 2.5: wrote the `.split("\n").map(trim).filter(...)` parsing pipeline correctly after one demonstration. First attempt at the matching step tried to jam a `.find()` call in as a second argument to the trim `.map()` instead of as its own separate `.map()` step; after the two-pipelines explanation, correctly wrote a second, standalone `.map()` containing `fakeResults.find((result) => result.companyName.toLowerCase().includes(name.toLowerCase()))`. Task 4.2: reused `.map()` in a new context (backend, transforming names into typed `CompanyResult` objects instead of strings) — the mechanics of `.map()` itself were fine; the bug that surfaced was the separate block-body-return issue, tracked above. Task 4.3: reused `.find()` again in a third context (searching `batchRuns` by id) — same block-body-return bug recurred and was self-corrected, tracked above; the `.find()` usage itself (searching an array of objects by a field) was applied correctly. |
| Ternary operator (`condition ? a : b`) | practicing | 2026-08-30 | 2026-08-30 | Task 2.5: after being shown the shape (as the two-sided version of the already-known `&&` conditional rendering), correctly wrote `return match ? match : notFound;` to supply a fallback `CompanyResult` when `.find()` returns `undefined`. |
| Object destructuring (`const { x } = obj`) | understood | 2026-09-05 | 2026-09-05 | Task 4.2: first attempt at reading `companyNames` off `req.body` wrote `const companyNames = req.body` (the whole object, not the field); after being pointed back to the already-known component-props destructuring pattern, correctly wrote `const { companyNames } = req.body`. Task 4.3: same lesson, independently and correctly wrote `const { id } = req.params;` on the first try, no hint needed — a second, different source object, unassisted. |
| Arrow function block bodies & explicit `return` (`{ }` vs. implicit return) | understood | 2026-09-05 | 2026-09-05 | Task 4.2: wrote a `.map()` callback with a `{ }` block body that built a `temp`/`result` object but never returned it. First prediction of the output ("nothing") was incomplete — the real answer is an array of the same length filled with `undefined`. After the implicit-vs-explicit-return explanation, correctly identified `return temp;` as the fix. Task 4.3, same lesson: hit a subtler version in a `.find()` callback — added `return batch;` (the object, always truthy) instead of returning the `===` comparison. Correctly predicted, unprompted, that this would make `match` always equal the *first* element of `batchRuns` regardless of the requested id, then independently rewrote it as `return batch.id === id;` — genuine forward reasoning about the mechanism, not just pattern-matching a fix. |

## Structural (files talking to each other, dependencies, package.json)

| Concept | Status | Introduced | Last reviewed | Evidence |
|---|---|---|---|---|
| Modules / imports & exports | understood | 2026-08-26 | 2026-09-05 | Task 2.2: user wrote a real `import BatchInputForm from "./components/BatchInputForm"` themselves, correctly using default-import syntax (no curly braces) after it was distinguished from `fakeResults.ts`'s named-export style. Review (2026-09-05): asked a forward-looking question (what would need to change if `BatchRun` had to be used from a new `routes/batches.ts` file) with no code in front of them to crib from — correctly answered "we need to export it and the new file needs to import it," unprompted. |
| React components & JSX (editing existing markup) | practicing | 2026-08-24 | 2026-08-28 | Task 2.2: filled in a full controlled-form component (label/textarea/button, event handlers) from a skeleton, then correctly rendered it inside `App.tsx`. |
| React state (`useState`) & controlled inputs | practicing | 2026-08-28 | 2026-08-30 | Task 2.2: correctly filled `useState("")`, `setCompanyText(event.target.value)`, and `console.log(companyText)`. Initial verbal explanation had `onChange`/`onSubmit` timing backwards ("click sets companyText, then submit prints it"); after correction, correctly answered that typing "Acme" calls `setCompanyText` 4 times (once per keystroke) and 0 times on submit. Task 2.5: first attempt at a new `useState<CompanyResult[]>([])` call reached for `CompanyResult[] \| null` then a malformed `<CompanyResult[] \| []>` type; corrected after being pointed at the type-vs-value distinction to the right call unassisted. |
| Rendering lists with `.map()` and the `key` prop | practicing | 2026-08-28 | 2026-08-28 | Task 2.3: correctly keyed each `<tr>` with `result.id` on the first try; initially placed `{result.companyName}` as a direct child of `<tr>` instead of inside a `<td>` (invalid table markup that happened to still render) — corrected after the HTML-structure explanation, confirmed in the saved file. |
| Props & callback props (parent ↔ child communication) | practicing | 2026-08-28 | 2026-08-30 | Task 2.4: correctly wired `ResultsTable`'s `onClick={() => { onSelectResult(result); }}` on the first try. In `App.tsx`, initially wrote `onSelectResult={() => setSelectedResult(selectedResult)}` — a callback that declared no parameter, so the row's data passed in had nowhere to bind and the line was a no-op. After tracing through why (asked to predict clicking Initech would show Initech's data; predicted wrong), correctly identified the fix needed a parameter and fixed it to `(result) => setSelectedResult(result)`. Task 2.5: converting `ResultsTable` to take a `results` prop, twice wrote it as a second function parameter (`function ResultsTable({onSelectResult}: Props, {results}: Props)`) instead of one merged destructured object — corrected after being pointed at `CompanyDetail`'s single-parameter pattern. Independently wrote `BatchInputForm`'s new `onSubmit` prop (interface, destructuring, and call site) correctly on the first attempt. |
| Lifting state up (shared state in the common parent) | practicing | 2026-08-28 | 2026-08-30 | Task 2.4: `selectedResult` state was placed in `App.tsx` (the shared parent of `ResultsTable` and `CompanyDetail`) rather than in either child, following the explanation of why sibling components can't share state directly. Task 2.5: initially answered that new `results` state should live inside `ResultsTable` itself ("it only needs to show it"); after being asked which components need to *write* vs. *read* it, self-corrected to `App.tsx` and could state why (siblings can't hand data to each other directly). |
| Conditional rendering in JSX (`&&`) | practicing | 2026-08-28 | 2026-09-05 | Task 2.4: correctly added `{selectedResult && <CompanyDetail result={selectedResult} />}` in `App.tsx` on the first try; confirmed working by clicking between two different rows in the browser and watching the detail view switch. Review (2026-09-05): predicted, incorrectly, that `results && <ResultsTable .../>` would not render when `results` is `[]` — a real gap in JS truthy/falsy rules (only `false, 0, "", null, undefined, NaN` are falsy; all objects/arrays, empty or not, are truthy). Confirmed understanding verbally after the correction, not yet re-tested independently. |
| `className` (linking JSX markup to CSS) | practicing | 2026-08-30 | 2026-08-30 | Task 2.6: after being told `class` is reserved in JS so JSX uses `className` instead, correctly added `className="company-detail"` to `CompanyDetail`'s wrapper `<div>` on the first try. |
| `useEffect` (side effects, dependency array) | practicing | 2026-09-03 | 2026-09-03 | Task 3.5: first prediction on the missing-dependency-array case was wrong (guessed it wouldn't run at all; corrected — it reruns after every render). Then, writing the real effect, initially omitted the empty array (`useEffect(() => {...})` with no `[]`) — a live instance of the exact mistake just corrected — but self-caught and fixed it unassisted after being pointed back at the pattern, without being told the specific bug. Confirmed working: data fetched and displayed once in the browser. |
| CSS layout & selectors (flexbox, `border-collapse`, `:hover`, custom properties) | introduced | 2026-08-30 | 2026-08-30 | Task 2.6: user asked the agent to write the styling pass directly rather than derive each property ("css isn't programming"); agreed to the offered middle ground (agent writes it, user pastes it into the file and still verifies/predicts). User correctly predicted the form would stack vertically and table rows would show a pointer cursor before checking the browser, and confirmed the result matched. Property-level authorship belongs to the agent here, not the learner — flagged `introduced` rather than `practicing` for honesty; revisit with a learner-authored styling task before calling this `practicing`. |
| npm packages & dependencies | understood | 2026-08-24 | 2026-09-07 | Task 1.4: correctly predicted `npm install` would produce `package-lock.json` in addition to `node_modules`, and that `npm run dev` would keep running rather than return the prompt. Ran the real install, confirmed both files appeared. Task 3.1: in a brand-new project (`backend/`), correctly predicted `node_modules/` and a `dependencies` block (with express + version) would appear after `npm install express` — transfer to a second project, not just repetition. Review (2026-09-07, forward-looking, no code in front of them): correctly recalled both artifacts unprompted before installing `pg`, then correctly predicted `pg`/`@types/pg` would land in `dependencies`/`devDependencies` respectively, confirmed against the real file. When pushed on *why* `node_modules` isn't committed, first said "too many files," then independently landed on the deeper reason (it's fully reproducible from `package.json`/`package-lock.json`) after one nudge — a correct, mostly self-driven explanation. |
| package.json | practicing | 2026-08-24 | 2026-08-31 | Read the real generated file with user; walked through `scripts` and `dependencies`/`devDependencies` blocks as part of task 1.3. Task 3.1: in the new `backend/` project, correctly predicted the fresh `npm init -y` output (name/version/scripts, no dependencies yet), then correctly predicted the `dependencies` block appearing after installing express and a `devDependencies` block after installing the dev tools — real transfer to a second, independent project. |
| package-lock.json (lockfiles & version pinning) | introduced | 2026-08-24 | 2026-08-24 | User predicted the file would appear but didn't know its purpose; explained it pins exact installed versions (vs. `package.json`'s loose ranges) for reproducibility, and that unlike `node_modules` it's committed to Git. |
| devDependencies vs. dependencies (runtime vs. dev-only packages) | practicing | 2026-08-31 | 2026-08-31 | Task 3.1: after being told `-D`/`--save-dev` splits installs into a separate block, correctly predicted a `devDependencies` block would appear (distinct from the `dependencies` block from the plain `npm install express`) after `npm install -D typescript @types/node @types/express tsx`; confirmed against the real file. |
| tsconfig.json / TypeScript compiler config | practicing | 2026-08-31 | 2026-09-07 | Task 3.1: correctly predicted `npx tsc --init` would produce a large, mostly-commented-out file. Hand-edited `"types": []` to `"types": ["node"]` themselves. Predicted the consequence of leaving it empty in the wrong direction (guessed it'd silently compile bad code); corrected to the real mechanism — TypeScript would instead error with "Cannot find name 'process'" since it wouldn't know Node's globals exist at all. Weekly review (2026-09-07): asked to predict TypeScript's reaction to a future unrecognized Node global in a new file; correctly recalled both the error type ("cannot find name") and the fix location (`tsconfig.json`'s `types` array), unprompted. |
| npx (running a local package's CLI without a global install) | introduced | 2026-08-31 | 2026-08-31 | Explained while running `npx tsc --init` (task 3.1) — runs a binary from `node_modules` without installing it globally. Not yet independently restated by user. |
| HTTP requests (fetch, request/response) | practicing | 2026-09-03 | 2026-09-03 | Task 3.5: wrote `fetch("http://localhost:3001/health")` and `response.json()` correctly on the first attempt after one explanation; correctly predicted the resolved `data` would be the `{ status: "ok" }` object. Confirmed working — real "ok" text rendered in the browser, fetched live from the backend. |
| Frontend/backend separation (client-server model) | practicing | 2026-08-23 | 2026-09-03 | Explained via core components #2/#3, reinforced by the Next.js tradeoff discussion in Decision 3. Task 3.5: first real, concrete demonstration — React app (port 5173) successfully fetched and displayed data from the independent Express server (port 3001), the actual client-server split working live rather than just described. |
| REST API endpoints (routes & HTTP requests) | understood | 2026-08-23 | 2026-09-07 | Task 3 review question: unprompted, correctly distinguished GET ("retrieve some data from the server") from POST ("used to write data to the server... update health data... and store that") — a real, correct free-recall explanation of what the HTTP verb signals. Weekly review (2026-09-07): asked which verb fits a partial user-profile update better than POST; first answer named PUT but described PATCH's actual behavior (send only the changed fields). On a follow-up asking what PUT's own name implies about omitted fields, self-corrected: "I think I confused it with PATCH" — correctly landed on PUT = full replace vs. PATCH = partial, on the second attempt. |
| Express server basics (`express()` app instance, `app.get`, `req`/`res`, `res.json`, `app.listen`) | practicing | 2026-09-03 | 2026-09-03 | Task 3.2: after each piece was explained separately (app creation, route registration, listen), user wrote the entire `server.ts` file unassisted in one pass — correct import, app created before the route, route registered before `listen()`, `res.json({ status: "ok" })` matching the target response. Confirmed working: predicted and then observed the exact startup log, then predicted and confirmed the browser response at `/health`. First contact, capped at `practicing` despite the clean first attempt. |
| Middleware (`app.use`, cross-cutting request handling) | practicing | 2026-09-03 | 2026-09-05 | Task 3.4: explained as "runs on every request, before your routes, regardless of path/method"; user correctly placed `app.use(cors())` after `const app = express()` and before the route when writing the code, but this followed direct instruction rather than an independent free-recall explanation. Task 4.2: added a second, different middleware (`app.use(express.json())`) correctly on the first try in the same spot — real transfer of "same pattern, different function" rather than copying the exact prior line. |
| CORS (Cross-Origin Resource Sharing, same-origin policy) | introduced | 2026-09-03 | 2026-09-03 | Task 3.4 review question: first predicted skipping CORS would cause a 404 (confused it with a routing failure); after correction, second prediction ("the browser stops the request before it's sent") was also wrong for a simple GET — the request is actually sent and answered, and the browser withholds the *response* from the JS. Needed a full explanation both times; real gap, not yet independently restated correctly. |
| Node module systems (CommonJS vs. ES modules, `package.json` `"type"` field) | introduced | 2026-09-03 | 2026-09-03 | Task 3.4 (side quest): user independently changed `backend/package.json`'s `"type"` from `"commonjs"` to `"module"` on their own initiative, reasoning "commonjs is meant for older versions of node" — plausible-sounding but not the real mechanism (CommonJS isn't deprecated; the field controls which import/export syntax Node expects). After the correct distinction was explained (matches the `import`/`export` syntax already used in `server.ts`), confirmed the server still ran correctly under the new setting. |
| Express `POST` routes & `req.body` | practicing | 2026-09-05 | 2026-09-05 | Task 4.2: wrote `app.post("/batches", (req, res) => {...})` from the already-known `app.get` pattern; built the full handler (destructure body, `.map()` to placeholders, construct `BatchRun`, `.push()` to the store) largely independently, with hints on the destructuring step. Caught, after being asked what the frontend would actually receive, that the final `res.json({ status: "ok" })` didn't send back the real `batch` object — fixed to `res.json(batch)` once the gap was pointed out. Task 4.4: manually confirmed working in Postman — sent a real JSON body and got back the exact predicted shape, a `200` with the full `BatchRun` including nested `CompanyResult`s. |
| Node `crypto.randomUUID()` (built-in unique ID generation) | practicing | 2026-09-05 | 2026-09-05 | Task 4.2: used `randomUUID()` (imported via `import { randomUUID } from "crypto"`) to generate `id` fields for both `CompanyResult` and `BatchRun`. First attempt called it as `crypto.randomUUID()` despite importing the function by name directly; corrected to `randomUUID()` once the mismatch was pointed out. |
| Express route parameters (`:id` in a path, `req.params`) | practicing | 2026-09-05 | 2026-09-05 | Task 4.3: first attempt registered the route as plain `"/batches/"` with no `:id` placeholder; after being asked where `:id` needed to go, corrected it to `"/batches/:id"` unassisted and correctly destructured `const { id } = req.params;`. Task 4.4: manually confirmed working in Postman — correctly predicted a `GET` to the outer `BatchRun` id would return that same run, and confirmed it did. |
| Manual API testing (Postman/curl — sending a request body, reading status + JSON response) | understood | 2026-09-05 | 2026-09-08 | Task 4.4: used Postman (prior general familiarity, first use *in this project*) to test both routes. Correctly predicted the general shape of the `POST /batches` response before sending; correctly predicted the exact `GET /batches/:id` found-case response; correctly predicted the `404` status for an unknown id (didn't fully specify the error body up front, but confirmed it matched once seen). First real evidence of testing an API surface manually rather than only via the frontend. Task 5.7: independently designed and ran a harder test — `POST /batches`, killed the server with `Ctrl+C`, restarted it with `npm run dev`, then `GET` the same id — to prove restart-persistence, not just request/response shape. Reported the exact response at each step unprompted. Second, distinct real-world use of the tool, applied to a new kind of question — bumped to `understood`. |
| HTTP status codes & `res.status()` | practicing | 2026-09-05 | 2026-09-05 | Task 4.3: after being told `res.json()` alone defaults to `200` and `res.status(404)` signals "not found," correctly wrote `res.status(404).json({ error: "not found" })` for the no-match branch on the first attempt. |
| Route parameters vs. query strings (`/path/:id` vs. `?id=`) | practicing | 2026-09-05 | 2026-09-05 | Post-4.4 side quest: tried testing `GET /batches/:id` in Postman using the Params tab (key `id`) instead of putting the id in the path, producing `.../batches/?id=...` and an HTML 404 page instead of the expected JSON. Asked to inspect the real URL, correctly identified nothing followed `/batches/` before the `?`; correctly reasoned this doesn't match `/batches/:id` at all. Correctly identified the HTML (vs. JSON) 404 body as proof Express's own default handler answered — the route never ran — not a `.find()` mismatch inside the handler. |
| TypeScript type inference (types deduced from context, no annotation needed) | practicing | 2026-09-05 | 2026-09-05 | Asked (unprompted by any task) whether `match` in `batchRuns.find(...)` needed an explicit type. Correctly predicted, without being told, that TypeScript would refuse to compile `match.results.length` written right after the `.find()` call, before the `if (match)` check — correctly reasoning from `match`'s inferred `BatchRun \| undefined` type that the possibility of `undefined` blocks the property access. |
| Relational database & schema design | understood | 2026-08-23 | 2026-09-08 | User's Decision 4 explanation: "Postgres is a relational database which defines relationships between the data. The data that would be stored is very relational" — confirmed correct. Review (2026-09-05): unprompted, correctly explained that restarting the server right now loses all batches (in-memory), and that storing in Postgres (Section 5) is what changes that — a clean forward-looking free-recall answer. Task 5.7: that exact prediction was put to a real test — correctly predicted, unprompted, that a batch would survive a full server restart "because we are storing it in the database so the state isn't saved in-memory but in an actual database that survives even when the server is closed," then confirmed it live via `Ctrl+C` → `npm run dev` → `GET`. |
| pgvector (embeddings stored in Postgres) | understood | 2026-08-23 | 2026-09-06 | User's Decision 4 explanation explicitly named it: "supports storing RAG pipeline needs so we don't need a vector database" — confirmed correct. Review (2026-09-06): correctly restated it unprompted at the start of Section 5 — "postgres has built in pgvector so it stores our data and the rag embeddings without needing another vector database." |

## Databases (SQL, schema design, Postgres syntax)

| Concept | Status | Introduced | Last reviewed | Evidence |
|---|---|---|---|---|
| SQL `CREATE TABLE` (columns, types, comma-separated definitions) | practicing | 2026-09-06 | 2026-09-06 | Task 5.2: wrote `CREATE TABLE batch_runs` from scratch — the first real SQL DDL in this project. Needed several rounds of correction (an invalid MySQL-style `enum()`, a trailing comma) before reaching valid syntax; by the second table (`company_results`), reproduced the same column/comma structure correctly on the first attempt — real transfer within the same lesson. |
| SQL data types matching app-level types (`uuid`, `text`) | practicing | 2026-09-06 | 2026-09-06 | Task 5.2: chose `uuid` for `id` (matching `crypto.randomUUID()` already used in `server.ts`) after discussion, then independently applied the same type to `company_results.id` and `batch_run_id` without being told again. |
| `PRIMARY KEY` | practicing | 2026-09-06 | 2026-09-06 | Task 5.2: correctly placed `PRIMARY KEY` directly after the `id uuid` column definition on both tables, including the second one unassisted. |
| `CHECK` constraints (restricting a column's allowed values — the DB-level equivalent of a TS union type) | practicing | 2026-09-06 | 2026-09-06 | Task 5.2: a real struggle on the first table — attempted MySQL-style `enum(pending, done)` (invalid in Postgres), then `CHECK ('pending' \| 'done')`, then `CHECK ('pending' OR 'done')` (neither actually referencing the column), before landing on `CHECK (status IN ('pending', 'done'))` after an analogous worked example (a `color` column). Applied the correct pattern unassisted to the second table's three-value `status` column on the first attempt — real evidence it landed despite the rocky first pass. |
| `FOREIGN KEY` / `REFERENCES` (linking a child row to its parent table) | practicing | 2026-09-06 | 2026-09-07 | Task 5.2: first attempt wrote `batch_run_id uuid FOREIGN KEY (batch_run_id) REFERENCES batch_runs(id)` — invalid, missing the comma a separate table-level constraint would need. After being shown the simpler inline column-constraint form (matching how `PRIMARY KEY` was already written), correctly simplified to `batch_run_id uuid REFERENCES batch_runs(id)`; confirmed working via `psql`'s `\d company_results`, listed under "Foreign-key constraints." Task 5.4: hit the constraint for real — a live `insert or update...violates foreign key constraint` error, since the code inserted `company_results` rows before the referenced `batch_runs` row existed. Correctly read the error's `detail` field and identified the fix direction unprompted ("batch_runs needs to be inserted before"); after one guiding question about what `batch_runs` actually needs to insert its row (just `id`/`status`, not `results`), independently restructured the handler to insert `batch_runs` first. Real evidence that a foreign key is enforced live at insert time, not just a schema-time annotation. |
| `CREATE TABLE IF NOT EXISTS` (idempotent re-runs, and its limits) | practicing | 2026-09-06 | 2026-09-09 | Task 5.2: independently proposed the exact right fix, unprompted, when asked what would happen re-running the schema file against an already-existing `batch_runs` table — "we need to add something like if doesn't exist." Predicted only "CREATE TABLE" would print on the re-run and missed the `NOTICE` line that actually appeared above it; the "printed success but nothing was actually recreated" distinction was explained rather than independently spotted. Task 6.1: re-ran the file after adding a third table (`users`); correctly predicted the two existing tables would be skipped but still print `CREATE TABLE` (all 3 "regarded as success") — a real improvement on the core mechanism. When asked directly whether the `NOTICE` lines themselves were expected, honestly said "i forgot about that" — the specific `NOTICE`-line detail still hasn't stuck on a second encounter, even though the more important skip-but-succeeds mechanism has. |
| `psql` meta-commands (`\d`, describing a table) | practicing | 2026-09-06 | 2026-09-06 | Task 5.2: correctly predicted, unprompted, that a `CHECK` constraint would appear in a separate section rather than inline on the column's row, confirmed against real `\d batch_runs` output. |
| Parameterized SQL queries (`$1`, `$2` placeholders) & SQL injection | practicing | 2026-09-07 | 2026-09-07 | Task 5.4: after SQL injection was explained as the risk of building queries via string-splicing, wrote `pool.query("INSERT INTO batch_runs (id, status) VALUES ($1, $2)", [batch.id, batch.status])` correctly on the first attempt, and reused the same pattern independently for the `company_results` insert (with a correction to the placeholder count after being asked to compare column list vs. `VALUES (...)` count). Task 5.5: extended the pattern to real `SELECT ... WHERE ... = $1` reads (`batch_runs` by `id`, `company_results` by `batch_run_id`), writing both correctly on the first attempt — real transfer from write-side to read-side parameterization. |
| SQL `UNIQUE` constraint (single-column uniqueness, distinct from `PRIMARY KEY`) | practicing | 2026-09-09 | 2026-09-09 | Task 6.1: after `UNIQUE` was distinguished from `PRIMARY KEY` (uniqueness-only vs. uniqueness-plus-identity), first attempt wrote `email UNIQUE NOT NULL` with no data type at all; self-corrected to `email text UNIQUE NOT NULL` once asked to compare the line against the three-part name/type/constraint pattern already used in `batch_runs`/`company_results`. Confirmed via `psql \d users` showing a real `users_email_key` UNIQUE index. First contact, capped at `practicing`. |
| SQL `timestamptz` type & `DEFAULT` values (auto-populated columns) | practicing | 2026-09-09 | 2026-09-09 | Task 6.1: first attempt wrote a bare `timestamptz DEFAULT now()` line with no column name (same missing-piece error as the `email` line, in the same attempt); self-corrected to `created_at TIMESTAMPTZ DEFAULT NOW()` once pointed at the same three-part pattern. Confirmed via `psql \d users` showing `created_at` as nullable with a real `now()` default. First contact, capped at `practicing`. |
| Postgres unquoted identifier case-folding (`companyName` → `companyname` at parse time) | understood | 2026-09-07 | 2026-09-07 | Task 5.4: after fixing the FK-order bug, independently changed `companyName`/`emailDraft` to `company_name`/`email_draft` in the `company_results` insert to match the schema's snake_case columns — correct fix, but applied without stating the reasoning aloud. Task 5.5, same day: the flagged "direct check" landed — asked why real query rows came back with `company_name`/`email_draft` instead of the `<CompanyResult>` generic's claimed `companyName`/`emailDraft`, correctly self-diagnosed unprompted: "is it because i did `<CompanyResult>` when making the query so it is expecting a CompanyResult type but CompanyResult doesn't have company_name but that is what is returned by the query" — a genuine, independent explanation of the real mechanism this time. |
| TypeScript generics as compile-time assertions, not runtime transforms (`pool.query<T>(...)` doesn't rename/validate anything) | practicing | 2026-09-07 | 2026-09-07 | Task 5.5: predicted (correctly, in general shape — "type error") that `pool.query<CompanyResult>(...)` would cause a compile error once `row.company_name`/`row.email_draft` were read against a type that only declares `companyName`/`emailDraft`; confirmed via real `npx tsc --noEmit` output (`TS2551: Property 'company_name' does not exist... Did you mean 'companyName'?`). Correctly identified the fix was to drop the inaccurate generic, not to revert the (correct) runtime property access. Then, unprompted, asked whether the same imprecision existed on `pool.query<BatchRun>(...)` for the `batch_runs` query (`BatchRun` includes a `results` field the raw row doesn't have) — real, independent transfer of the pattern to a second query, followed by a judgment call to remove it there too. |
| Array index access & control-flow narrowing (`arr[i]` is `T \| undefined`; checking `.length` doesn't narrow a *different* expression) | practicing | 2026-09-07 | 2026-09-07 | Task 5.5: hit a real `Type 'BatchRun \| undefined' is not assignable to type 'BatchRun'` error on `const { status }: BatchRun = result.rows[0]`, despite an earlier `if (result.rows.length <= 0) return;` check. After the explanation (TypeScript narrows the exact expression you check, not a related-but-different one), correctly restructured it to check `batchRow` (the actual `result.rows[0]` value, stored in a variable) directly, resolving the error — confirmed via a clean `npx tsc --noEmit`. First contact, capped at `practicing`. |
| Connection pooling (`pg`'s `Pool`, one reusable set of open connections vs. one per query) | practicing | 2026-09-07 | 2026-09-07 | Task 5.3: after the concept was explained (reuse a small set of open connections rather than opening a new one per query), wrote `export const pool = new Pool({ connectionString: process.env.DATABASE_URL })` in `backend/db/pool.ts` correctly on the first attempt, plus the matching `import { Pool } from "pg"`. First contact, capped at `practicing`. |
| Node built-in `.env` loading (`process.loadEnvFile()`) & load-order dependency | practicing | 2026-09-07 | 2026-09-07 | Task 5.3: wrote `process.loadEnvFile()` before constructing `Pool` in `pool.ts`, in the correct order, on the first attempt. When asked directly what would break if the order were swapped, correctly predicted "the connection would fail" and, after being asked to be precise about the mechanism, correctly reasoned that `process.env.DATABASE_URL` would still be `undefined` at the moment `Pool` is constructed if `loadEnvFile()` hadn't run yet. |
| Node ESM relative-import extensions under `"module": "nodenext"` (`.js` in the import path for a `.ts` file) | introduced | 2026-09-07 | 2026-09-07 | Explained directly as a config quirk of `tsconfig.json`'s `"module": "nodenext"` setting when importing `pool.ts` into `server.ts` (`from "./db/pool.js"`); user applied it correctly as given, not yet independently derived or restated. |
| Database transactions (`BEGIN`/`COMMIT`/`ROLLBACK`, atomicity) & `pool.connect()` for a dedicated client | practicing | 2026-09-08 | 2026-09-08 | Task 5.6 (self-initiated addition, ahead of the plan): asked to predict whether wrapping a route in `BEGIN`/`COMMIT` while the insert helpers still called the shared `pool.query()` would keep those inserts inside the transaction; first answer was wrong ("yes, because pool pools all the connections together") — corrected that `pool.query()` checks out and returns a *different* connection per call, so transaction state (tied to one session) doesn't carry over; the real fix is one dedicated client via `pool.connect()`, used for every query in the transaction. Correctly predicted, unprompted, that concurrent `client.query()` calls via `Promise.all` on one shared client would run sequentially rather than error or interleave (`pg` queues them client-side) — landed this one on the first try. Independently rewrote both insert helpers to accept a `PoolClient` parameter and wired `BEGIN` → insert loop → `COMMIT` in the route, with `ROLLBACK` + `client.release()` in `catch`/`finally`. Found the missing `await` on `client.query("ROLLBACK")` unprompted during self-review, but first misdiagnosed the consequence ("it would prevent `finally` from being called" — backward; `finally` isn't gated by an unawaited promise at all) before being corrected that the real risk is `release()` returning the connection to the pool *before* the rollback finishes on the wire. Verified atomicity with a real test (assistant-executed, user-directed): sent a batch with a `null` company name to trip the `company_name NOT NULL` constraint mid-insert, confirmed a `500` response, and confirmed via `psql` row counts (`batch_runs`/`company_results`) unchanged before/after — including the row that had inserted successfully before the failure hit. Capped at `practicing`: real, largely independent implementation with two corrected mispredictions along the way, but the verification queries themselves weren't typed by hand. |

## Engineering practice (git commits, testing, environment variables)

| Concept | Status | Introduced | Last reviewed | Evidence |
|---|---|---|---|---|
| Git commits & version history | understood | 2026-08-23 | 2026-09-03 | Task 1.6: rejected Claude's proposed commit message and supplied their own ("Create React app with Vite") — shows real ownership of what a commit message should communicate, not just mechanical add/commit. Review (2026-09-03): unprompted, correctly explained small commits isolate what changed so a breaking change can be pinpointed and reverted, vs. a large commit making debugging harder — a clean, correct free-recall explanation. |
| .gitignore (excluding files from version control) | practicing | 2026-08-24 | 2026-09-07 | Task 1.6: user correctly predicted, in their own words, that `node_modules` would not be staged "because it is in the gitignore file so git will ignore that" — confirmed against real `git status` output showing it absent. Task 3.1: independently noticed (when prompted) that `backend/` had no `.gitignore` of its own and correctly predicted `node_modules` would get staged without one; correctly predicted, in a second case, that adding `node_modules` to a new `backend/.gitignore` would keep it out of `git status --untracked-files=all` — real transfer to a second, independent project's Git scope. Downgraded 2026-09-07 (task 5.3): predicted a freshly-gitignored `backend/.env` would still "show up as untracked" in `git status` — a real gap, since the actual behavior is that a gitignored file is invisible to `git status` entirely, not flagged-but-listed. Corrected against real `git status` output (compared directly to `backend/db/`, a genuinely untracked file, which did show up). Not yet independently re-restated since the correction. |
| GitHub (remote repo hosting) | understood | 2026-08-23 | 2026-09-03 | Task 1.2: repo created and pushed via `gh`. User pushed back on the private default with a real question, weighed the public-vs-private tradeoff against the project's portfolio goal, and chose public. Task 2.7: ran `git push -u origin feature/ui` themselves and correctly interpreted the output, but hadn't yet restated why `-u`/tracking matters. Task 3.6: closed that gap — correctly explained unprompted that `-u` sets up branch tracking, so a later plain `git push` (no flags) would still work because the branch now "lives on remote." |
| Feature-branch → PR → merge workflow | understood | 2026-08-30 | 2026-09-03 | Task 2.7: ran the full real cycle themselves for the first time in this project — `git push -u`, `gh pr create` (correctly predicted it would ask for a title, body, and target branch before running it), a real self-review of the PR diff on GitHub, and squash-merged via the GitHub website. Task 3.6: drove the entire cycle independently and faster this time — stage, commit, `push -u`, `gh pr create`, merge, and delete both branches — with far less step-by-step guidance needed. Correctly predicted, unprompted, that `git add .` from inside `backend/` would only stage backend files (not the repo-root changes), and explained why a later plain `git push` would work once `-u` set up tracking. |
| Merge strategies (merge commit vs. squash vs. rebase) | introduced | 2026-08-30 | 2026-08-30 | Task 2.7: agent explained the three options and recommended squash-and-merge; user accepted the recommendation ("i squashed and merged") without independently restating the reasoning — real usage, but not yet the user's own explanation. |
| Branch cleanup & squash-merge ancestry (`git branch -d` warnings) | practicing | 2026-08-30 | 2026-09-07 | Task 2.7: hit a real `git branch -d` warning ("not yet merged to HEAD") after a squash-merge; instead of panicking, ran `git log --oneline` and `git status` on request to check actual state, correctly reported both showing the squashed commit safely on `main` and a clean working tree — real evidence-based debugging, not just accepting reassurance. Weekly review (2026-09-07, forward-looking): asked what to check before a future `-D` if the same warning recurs; correctly named checking `git log`/`git diff` against `main` to confirm nothing unmerged before forcing the delete, unprompted. |
| Environment variables & secrets | practicing | 2026-08-23 | 2026-09-07 | Explained in core components #8 (secrets kept out of code that will be public on GitHub); not restated until task 5.3, where — unprompted, before any code was written — correctly proposed "env variables + gitignore" as the fix for hardcoding the database connection string, then created `backend/.env` and added it to `.gitignore` correctly on the first try. First real hands-on use in this project. |
| Authentication & sessions | practicing | 2026-08-23 | 2026-09-10 | Task 6.2: wrote `POST /signup` from scratch — destructured email/password, added input validation with a guard clause, used `bcrypt.hash()` to hash the password (correctly understanding it's async and needs `await`), performed a pre-check for duplicate emails via `SELECT`, and used `RETURNING` to get the inserted row back from a single `INSERT` query. Hit a real bug (missing `VALUES` in the insert query) and self-fixed it; independently predicted and then discovered the database-persistence lesson (rows commit even when the response crashes). First hands-on use of bcrypt and password hashing in this project; capped at `practicing` on the first attempt despite the successful implementation. |
| Password hashing (bcrypt) | practicing | 2026-09-10 | 2026-09-10 | Task 6.2: used `bcrypt.hash(password, 10)` correctly as an async operation, understanding the salt rounds parameter and the one-way nature of hashing. First real contact with the concept in this project. |
| Input validation (guarding against missing fields) | practicing | 2026-09-10 | 2026-09-10 | Task 6.2: added `if (!email || !password) { return res.status(400).json(...) }` to validate required fields, correctly understanding that the `return` statement ensures the function exits immediately and prevents further code execution. |
| SQL `RETURNING` clause (getting inserted rows back) | practicing | 2026-09-10 | 2026-09-10 | Task 6.2: added `RETURNING id, email` to an `INSERT` statement to get the newly-inserted row back in the same query (instead of doing a second `SELECT`), correctly reasoning that this avoids an unnecessary round-trip to the database. |
| HTTP `409 Conflict` status code | practicing | 2026-09-10 | 2026-09-10 | Task 6.2: used `res.status(409).json({ error: "Email already in use" })` to signal that a signup request conflicts with existing data (an email that's already registered). First real use of 409 in this project. |
| HTTP `400 Bad Request` status code | practicing | 2026-09-10 | 2026-09-10 | Task 6.2: used `res.status(400).json({ error: "Email and password are required" })` to signal malformed input (missing required fields). Understood that 400 means the request itself is invalid, vs. 409 (the request is valid but conflicts with existing state). |
| Database writes persist even on response failure | understood | 2026-09-10 | 2026-09-10 | Task 6.2 discovery: a row committed to the database (via a successful `pool.query()`) persists even if a crash on the very next line prevents an HTTP response from being sent. Independently predicted and then verified via real `psql` inspection — two `users` rows confirmed, one from the crash attempt and one from the successful retry. Critical lesson for understanding transaction scope and why wrapping multi-step operations in `BEGIN`/`COMMIT` matters. |
| Centralized error handling (one try/catch in the route vs. scattered try/catches in helpers) | practicing | 2026-09-10 | 2026-09-10 | Task 6.2 refactor: asked to review `POST /signup`'s error handling; correctly predicted, after being told about Express 5's automatic async-error forwarding, that letting helpers (`findUser`/`hashPassword`/`createUser`) throw naturally and catching once in the route (matching the existing `POST /batches` pattern) was the right structure. Independently executed the full refactor — removed three inner `try`/`catch` blocks and added one wrapping `try`/`catch` around the route body — correctly on the first attempt, then independently identified and removed a now-dead `if (!newUser)` check, correctly reasoning that `createUser` can now only ever return a real value or throw (never return falsy), so the route's own `catch` handles the failure case before that line could matter. |
| Express 5 automatic async error forwarding (rejected promises in route handlers → default error handler, not a process crash) | introduced | 2026-09-10 | 2026-09-10 | Task 6.2 refactor: first prediction (an uncaught error would crash the whole server) was a reasonable but dated instinct from Express 4/raw Node behavior; corrected — Express 5 automatically catches rejected promises from async handlers and forwards them to its default error handler, so the process survives, but the response breaks the app's consistent JSON-error contract (an HTML default error page instead of `{ error: ... }`). Not yet independently restated, but correctly applied the practical consequence (centralize error handling in the route) right after the correction. |
| Background jobs / async processing | introduced | 2026-08-23 | 2026-08-23 | Explained in core components #7; not yet restated by user. |
| Testing (unit tests) | seed | — | — | Named only as a build-plan deliverable (section 8); concept itself not yet explained. |
| Bcrypt library & Node crypto packages generally | practicing | 2026-09-10 | 2026-09-10 | Task 6.2: installed `bcrypt` as a runtime dependency and `@types/bcrypt` as devDependency, understanding the split. First hands-on use of a specialized crypto package; prior exposure to `crypto.randomUUID()` as Node built-in. |
| Hot Module Reload / React Fast Refresh | practicing | 2026-08-24 | 2026-08-24 | Task 1.5: user predicted the counter's `useState` value would reset to 0 on a live edit; corrected — Fast Refresh preserves component state across edits. Ran a real test (clicked to 3, edited the file, count stayed 3) confirming the correction. |
| Deployment & hosting (Vercel/Railway) | understood | 2026-08-23 | 2026-09-06 | User's Decision 5 explanation, confirmed correct: "Vercel is built for deploying frontend apps... Railway provides hosting for the backend and database so you don't need separate hosting for each." Review (2026-09-06): correctly answered that the local Postgres just set up is not where real users' data will live — the deployed app needs its own separate, hosted database. |
| Homebrew (system-wide package manager, unlike project-scoped npm) | introduced | 2026-09-06 | 2026-09-06 | Task 5.1: initial prediction conflated `brew install postgresql@17` with `npm install`'s project-scoping ("it would install onto the project"); corrected to system-wide/global. Not yet independently restated. |
| Postgres "cluster" vs. individual databases (one server process holds many databases) | introduced | 2026-09-06 | 2026-09-06 | Task 5.1: explained when Homebrew's install output referenced a "default database cluster"; not yet independently restated. |
| Background services & port conflicts (launchd, `ps aux`, reading a crash log to find a root cause) | practicing | 2026-09-06 | 2026-09-06 | Task 5.1 (unplanned debugging detour): a forgotten PostgreSQL 15 install from months earlier (a system `LaunchDaemon`, running since June) was silently occupying port 5432, causing the fresh Postgres 17 to crash on start and misdirecting `createdb`'s connection to the wrong, differently-configured server. User ran each diagnostic command themselves (env vars, `pg_hba.conf`, `lsof`, `ps aux`, the actual crash log) and correctly predicted the process list would shrink after `launchctl unload`; correctly reasoned through the evidence at each step (e.g., recognizing `pg_hba.conf` said "trust" everywhere, which meant the failing connection couldn't be reaching this server) rather than guessing. Diagnostic sequence was agent-directed, not self-initiated, hence capped at `practicing` rather than `understood`. |
| Unix convention: silent success, output on failure | introduced | 2026-09-06 | 2026-09-06 | Task 5.1: predicted `createdb` would print a confirmation message on success; corrected — it printed nothing, which is the actual convention (most Unix tools stay quiet unless something goes wrong). |

## AI-era practice (writing a good plan, reviewing a diff, agent memory files)

| Concept | Status | Introduced | Last reviewed | Evidence |
|---|---|---|---|---|
| Retrieval-Augmented Generation (RAG), overall pattern | practicing | 2026-08-23 | 2026-09-09 | User's explanation collapsed the pipeline into "get company data → pass to LLM"; corrected that retrieval is a filtering step, not a pipe — attempted with help, not yet a clean pass. Review (2026-09-09): asked what would be missing if the pipeline just pasted a scraped page straight into the prompt instead of using RAG; answer described the feature's outcome ("uses RAG to scrape the site and generate a custom draft") but still didn't name retrieval/chunking/embeddings as the actual distinguishing mechanism — same gap as the original explanation, recurring unchanged after 17 days. Refresher given (retrieval filters to relevant chunks before generation, vs. prompt-stuffing the whole page). Worth a dedicated re-check closer to Section 7, not just passive review. |
| Chunking (splitting text for retrieval) | introduced | 2026-08-23 | 2026-08-23 | Explained as part of the RAG breakdown; not mentioned in user's own explanation. |
| Embeddings (vector meaning representations) | introduced | 2026-08-23 | 2026-08-23 | Explained as part of the RAG breakdown; not mentioned in user's own explanation. |
| Retrieval (semantic filtering to relevant chunks) | introduced | 2026-08-23 | 2026-08-23 | The specific gap corrected in the user's RAG explanation — heard, not yet correctly restated. |
| Generation (LLM drafting from retrieved context) | practicing | 2026-08-23 | 2026-08-23 | User correctly described this piece: "passing it to an llm to draft an email." |
| Scoping & sequencing a build plan (MVP vs. v2, phased sections) | practicing | 2026-08-23 | 2026-09-08 | Jointly built the MVP/parking-lot split and the 9-section build plan; user engaged directly in the tradeoffs, not yet independently restated. Review (2026-09-08): asked why auth (Section 6) is sequenced before the AI pipeline (Section 7) despite the pipeline being the actual product. First answer was surface-level ("so only authenticated users can access the pipeline") — didn't name the actual stakes. After one nudge ("what's uniquely risky about this pipeline being public, vs. a simple notes app?"), correctly landed on the real reason: the AI pipeline costs real money per call, so an unauthenticated public route lets anyone burn that budget. Still `practicing` — needed a nudge rather than free recall, but a genuine, correct explanation once prompted. |
| Agent memory / persistent context files (project.md, plan.md as living docs) | understood | 2026-08-23 | 2026-09-04 | User specified, unprompted, the exact mechanics wanted for this very file — status ladder, evidence fields, update triggers — demonstrating real grasp of the concept, not just use of it. Review (2026-09-04): first answer ("so we can check off tasks step by step") was only partial; on the follow-up ("would I remember these 6 tasks in a brand new conversation without reading plan.md?"), correctly answered "no" — landing the real point, that the agent has zero cross-session memory and the file is the only thing that persists. |
| Reviewing a diff / code review | practicing | 2026-09-03 | 2026-09-03 | Task 3.6: after merging without a pre-merge review, did a retrospective read of `git show a5cd8fd --stat` and correctly summarized the shape of the change (new `backend/` folder and files, `App.tsx` updated); missed that the `learning/` docs rode along in the same commit until pointed out. First real contact with reading a diff, capped at `practicing`. |
| Defensive naming (a name that makes incorrect usage look wrong, e.g. `password_hash` vs. `password`) | practicing | 2026-09-09 | 2026-09-09 | Task 6.1: asked why naming a column `password` (when it will only ever hold a hash) is risky. First answer reasoned the name is safe *because* hashing is the standard practice — missed that the real risk is future code, not a reader's assumption. After being shown the concrete failure mode (a column named `password` invites writing `INSERT ... (password) VALUES ($1)` with the raw string, with nothing about the name to stop it), correctly saw the point and renamed it to `password_hash` unprompted. First contact with this idea as an explicit concept. |

---

## Next quiz candidates (oldest/weakest first)

Reviewed 2026-09-07 (task 5.3): `npm packages & dependencies` (passed, bumped to `understood`). Also downgraded `.gitignore` back to `practicing` after a real wrong prediction mid-lesson (gitignored files are invisible to `git status`, not flagged-but-listed) — worth a dedicated re-check next time it's relevant, not just left to age.

Reviewed 2026-09-07 (weekly review): `TypeScript static typing` (passed, forward application to upcoming Postgres query typing), `REST API endpoints` (passed on second attempt — first answer swapped PUT/PATCH's behaviors, self-corrected when asked what PUT's own name implies), `tsconfig.json / TypeScript compiler config` (passed), `Branch cleanup & squash-merge ancestry` (passed, forward application to a future squash-merge).

Still stale (`practicing`/`understood`, last-reviewed 7+ days before 2026-09-07) and not yet reviewed: React JSX/list-rendering/props/lifting-state cluster (all last-reviewed 2026-08-28 to 2026-08-30), package.json/devDependencies vs. dependencies (2026-08-31), and the AI-era-practice cluster dated 2026-08-23 (RAG overall, Generation, scoping/sequencing a plan). Worth a dedicated review pass, especially the AI-era cluster once Section 7 (RAG) approaches — that's the one with the confirmed original gap (Retrieval vs. RAG-as-a-pipe).
