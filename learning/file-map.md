# Lead Scout — file map

Every file and folder in this project, one line each, so nothing in the repo is a mystery box.

**Status key:** `known` (explained in my own words) · `parked` (honest one-liner for now, deep dive scheduled) · `generated` (machine-made, never hand-edit).

| Path | What it is | Why it exists | Status |
|---|---|---|---|
| `lead-scout/` | Project root. | Everything for Lead Scout lives under here. | known |
| `lead-scout/learning/` | Folder holding the project's persistent planning/learning docs. | Read at the start of every session so context survives between sessions instead of getting re-derived. | known |
| `lead-scout/learning/project.md` | Who I am, the project idea and why it beat the alternatives, the MVP vs. v2 feature split, and the 8 core components. | The *what/why* record — read first, before any other file. | known |
| `lead-scout/learning/plan.md` | The five locked stack decisions (language, frontend, backend framework, database, hosting) and the 9-section build sequence with a concrete deliverable per section. | The *how* — turns the project.md idea into an ordered, checkable build path. | known |
| `lead-scout/learning/knowledge-graph.md` | Per-concept tracker (seed → introduced → practicing → understood) spanning low-level, structural, engineering-practice, and AI-era-practice concepts. | Drives what gets taught vs. quizzed next; prevents re-teaching what's already understood and re-testing what's already fresh. | known |
| `lead-scout/learning/file-map.md` | This file. | So every file in the repo has a stated purpose the moment it's added — nothing accumulates unexplained. | known |

| `lead-scout/.git/` | Hidden folder Git uses to store the repo's snapshot history. | Created by `git init` (task 1.1); this is what turns the folder into a tracked repo. | generated |

| `lead-scout/frontend/` | The React + Vite + TypeScript app, scaffolded via `npm create vite@latest` (task 1.3). | This is the "frontend" piece from the locked stack decision — the actual web page users touch. | known |
| `lead-scout/frontend/package.json` | The app's manifest: its name, runnable scripts (`dev`, `build`, `lint`, `preview`), and its dependency list (`react`, `react-dom`, plus dev tools like `vite`, `typescript`). | `npm install` (task 1.4) reads this to know what to download; `npm run dev` reads the `scripts` block. | known |
| `lead-scout/frontend/index.html` | The actual HTML page the browser loads. Just an empty `<div id="root">` and a `<script>` tag pointing at `main.tsx`. | React needs one real HTML page to mount into — this is it. | known |
| `lead-scout/frontend/src/main.tsx` | Entry point. Takes the React app and mounts it into `index.html`'s `<div id="root">`. | The bridge between the plain HTML page and the React component tree. | known |
| `lead-scout/frontend/src/App.tsx` | The main React component — what actually renders on screen. | Hand-edited in task 1.5 (heading/tagline); task 2.2 added `<BatchInputForm />`, task 2.3 added `<ResultsTable />`; task 2.4 added `selectedResult` state and conditionally renders `<CompanyDetail />`. User also removed the leftover Vite scaffold (counter, docs/social links) during 2.4. Task 2.5 added `results` state (starts `[]`) and a `handleSubmit` function that parses the pasted text into names and matches each against `fakeResults` (case-insensitive substring match), falling back to a "pending, no data" placeholder for unmatched names. | known |
| `lead-scout/frontend/vite.config.ts` | Configuration for Vite itself (dev server behavior, plugins like React support). | Controls how `npm run dev`/`npm run build` behave. | known |
| `lead-scout/frontend/tsconfig.json` (+ `tsconfig.app.json`, `tsconfig.node.json`) | TypeScript compiler settings, split by context (app code vs. Vite's own Node-side config). | Governs what TypeScript's type-checker enforces. | parked |
| `lead-scout/frontend/.gitignore` | List of paths Git should never track for this sub-folder (e.g. `node_modules`, build output). | Generated boilerplate; standard, not worth a deep dive yet. | parked |
| `lead-scout/frontend/.oxlintrc.json` | Config for `oxlint`, the linter wired into the `lint` script. | Generated boilerplate; not touched yet. | parked |
| `lead-scout/frontend/README.md` | Vite's default starter README. | Generated boilerplate — will likely get overwritten with real project docs later. | parked |
| `lead-scout/frontend/public/` (`favicon.svg`, `icons.svg`) | Static assets served as-is (favicon, icon sprite). | Generated boilerplate. | parked |
| `lead-scout/frontend/src/App.css` | Real styles for the form, table, and company-detail card (task 2.6) — flexbox stacking, table borders/`border-collapse`, row `:hover` highlight, `.company-detail` card. Cleared out the leftover Vite scaffold rules first (`.counter`, `.hero`, `.ticks`, etc. — none of it matched any current markup). | Written mostly by the agent at the user's request (a deliberate faster-paced tradeoff for CSS, not core logic); the user pasted it in, predicted the visual result correctly, and verified it in the browser. | known |
| `lead-scout/frontend/src/index.css` | Default starter styles — CSS custom properties (colors, fonts), the `#root` width constraint, base `h1`/`h2`/`p` styling. Untouched so far; `App.css`'s new rules reuse its `--border` and `--accent-bg` variables. | Generated boilerplate — not yet directly edited or fully toured. | parked |
| `lead-scout/frontend/src/assets/` | Folder for static assets imported into components (e.g. the default React logo). | Generated boilerplate. | parked |
| `lead-scout/frontend/src/data/fakeResults.ts` | Exports a `CompanyResult` interface and a `fakeResults` array of pretend company research results (task 2.1). | Lets Section 2 build the input form, results table, and detail view against realistic-looking data before any backend exists. | known |
| `lead-scout/frontend/src/components/` | Folder for individual React component files, one component per file (task 2.2). | Keeps `App.tsx` from becoming one giant file as more UI pieces get built. | known |
| `lead-scout/frontend/src/components/BatchInputForm.tsx` | A controlled form: a textarea (bound to `useState`) for pasting company names (task 2.2). Task 2.5 replaced the `console.log` with an `onSubmit` callback prop, called with the typed text on submit. | The first of Section 2's three real screens; now actually wired to `App.tsx`'s matching logic instead of just logging. | known |
| `lead-scout/frontend/src/components/ResultsTable.tsx` | Renders a table, one `<tr>` per company via `.map()`, keyed on `result.id` (task 2.3). Task 2.4 added an `onSelectResult` callback prop, called with a row's data on click. Task 2.5 changed it to take `results` as a prop instead of importing `fakeResults` directly, so it renders whatever `App.tsx` hands it. | The second of Section 2's three real screens; now displays real matched/typed results, not a fixed hardcoded list. | known |
| `lead-scout/frontend/src/components/CompanyDetail.tsx` | Takes a `result: CompanyResult` prop and renders its name, status, brief, and email draft (task 2.4). Task 2.6 added `className="company-detail"` to the wrapper `<div>` so `App.css` can style it as a card. | The third of Section 2's three real screens; only rendered once a row's been clicked in `ResultsTable`. | known |

| `lead-scout/frontend/node_modules/` | The actual downloaded code for every dependency in `package.json`, installed by `npm install` (task 1.4). | Node needs the real library code on disk to run/build the app, not just the list of what's needed. | generated |
| `lead-scout/frontend/package-lock.json` | Exact, pinned versions of every dependency (and their sub-dependencies) actually installed, generated by `npm install`. | `package.json` only lists loose version ranges; this file locks the precise tree so installs are reproducible across machines. Committed to Git (unlike `node_modules`). | generated |

| `lead-scout/.claude/` | Claude Code config for this repo — currently holds the `next-lesson` and `weekly-review` skill definitions that run these learning sessions. | Tooling for how the *lessons* run, not part of the Lead Scout app itself. | parked |
