# Lead Scout — project log

Read this file at the start of every session on this project.

## Who I am

- CS degree. Know Java, JavaScript, Python, HTML, CSS. Comfortable with OOP and syntax across languages.
- Node.js experience, React basics, SQL basics.
- Have built projects before, but leaned too heavily on AI-generated code or tutorials — didn't retain much, and get stuck not knowing where to start when building from scratch without a guide.
- Currently job hunting for SWE roles, primarily at startups.
- Longer-term goal: break into AI engineering. Want this project to include a real AI/RAG element, not a bolted-on LLM call, as genuine skill demonstration.
- Explicit goal for this project: learn to code for real — understand and retain what's built, not just produce a working demo.

## The idea

Lead Scout: take a batch of company names or domains and turn each one into a research brief plus a drafted cold outreach email — automating the "look up the company, find an angle, write a personalized opener" work that eats hours of manual outbound.

Chosen over two alternatives considered:
- A web-based cold-calling tool (WebRTC + Twilio) — rejected as MVP-worthy: it's telephony infra more than AI/data work, costs real money to test, carries TCPA/robocall compliance risk, and has no visible-early milestone (nothing demoable until the mic-to-PSTN pipeline fully works).
- Ranked against "Feedback Radar" (feedback clustering) and a generic RAG-docs-chatbot idea — Lead Scout won on strongest business ROI story, most natural full-stack surface area, and least "seen it a thousand times" AI angle.

Target user: startup/SDR doing manual outbound prospecting.

## MVP — the smallest version that's genuinely useful, live on the internet

1. Batch input — paste or upload a CSV of company names/domains. Batch size capped (e.g. 15) as a cost/failure guardrail, not permanent.
2. Real per-company research — search API + fetch static HTML from company site/news. No headless browser, no JS rendering.
3. Actual RAG — chunk retrieved text, embed it, store embeddings in pgvector (same Postgres, no separate vector DB service), retrieve top-k relevant chunks for generation.
4. Grounded generation — LLM writes a research brief + cold email draft, citing which retrieved chunk it drew from. Not raw prompt-stuffing.
5. Simple background processing — sequential worker so a batch doesn't block the HTTP request. No queue infrastructure yet.
6. Postgres — companies, batch runs, chunks/embeddings, drafts.
7. Real auth — signup/login/logout, sessions, single-tenant per user. Doubles as portfolio-required "auth" skill and as cost protection for the public deployment.
8. Minimal frontend — submit a batch, see run status, view results table, open a company for brief + editable draft text, export batch as CSV.
9. Actually deployed — frontend, backend, worker, DB all hosted (not localhost). Secrets in env vars. Batch-size cap and basic per-user rate limit so it survives being public.

## Parking lot (v2) — deliberately deferred, not forgotten

- Headless-browser scraping (Playwright) for JS-heavy/anti-bot sites
- Real job queue (Redis/BullMQ) with retries, concurrency, live progress
- Direct email sending via Gmail/Outlook OAuth
- CRM integrations (HubSpot, Salesforce, Airtable sync)
- Regenerate-with-instructions, tone/persona options, multiple draft variants
- Teams/orgs, roles, shared lists
- Password reset, email verification, SSO
- Analytics dashboard (reply rates, campaign stats)
- Multi-LLM provider support
- Usage-based billing/plans
- Bulk batches (100s–1000s of companies) with real cost controls
- Retrieval evaluation harness (precision/recall on retrieval quality)
- Real-time progress via websockets
- Saved/searchable lead lists across past runs
- Notifications when a batch finishes

## Core components (build roadmap)

The major pieces needed to build and deploy this end to end, roughly in the order they get built:

1. **Source control (Git + GitHub)** — Git saves snapshots ("commits") of the code as it changes, giving a history to fall back on. GitHub hosts that history online and is how the work gets shown to others. Set up before any code, so every other piece has a safety net from day one.
2. **Frontend** — the actual web page (forms, buttons, results table), built with HTML/CSS/JavaScript via React (a library for building pages out of reusable pieces called components). The only piece a human directly touches.
3. **Backend / API server** — a program that runs continuously on a server (an always-on computer that isn't the laptop) and does the real work: receives requests, decides what to do, talks to the database and outside services. "API" = the fixed menu of requests the frontend is allowed to send it. Keeps secrets and logic off the browser.
4. **Database (Postgres)** — storage that survives after a program stops, unlike in-memory data which vanishes on restart. Needed to remember companies, runs, and drafts across sessions/logins.
5. **Authentication** — login system: checks identity, then tracks "logged in" via a session (a proof-of-login token the browser holds). Needed both as the portfolio-required auth skill and as protection against strangers burning paid API budget once the app is public.
6. **The AI pipeline (RAG)** — the core product logic: search/fetch real facts about a company → chunking (cut text into small pieces) → embeddings (turn each piece into a comparable "meaning fingerprint") → retrieval (filter down to the pieces actually relevant to what's being written) → generation (hand those to an LLM to draft the brief/email). Retrieval is a genuine filtering step, not just a pipe from source text to LLM — dumping a whole scraped page into the prompt is prompt-stuffing, not RAG. This is the specific AI skill the project exists to demonstrate.
7. **Background job processing** — hands a slow batch of research off to run separately from the request, so the page shows "in progress" instead of freezing. Needed so batches don't time out or lock the UI.
8. **Deployment / hosting** — moving code off the laptop onto always-on servers reachable at a public address (separate hosts for frontend, backend, database), with secrets (API keys) stored as environment variables so they never get written into code that will sit publicly on GitHub.

## Decisions worth remembering

- Auto-sending emails was explicitly rejected for MVP and v2 consideration is not a given — deliverability/spam liability. Product is a draft a human sends, not an auto-sender.
- CRM sync was cut from MVP despite feeling "business-oriented" — CSV export proves the same integration-readiness without OAuth complexity.
- Local Postgres stays on "trust" auth (no password) rather than being switched to password-based now — anyone with access to the laptop already has more access than a local DB password would gate, so it protects against nothing real. Password auth becomes necessary once Postgres is networked on Railway (Section 9), where the hosted connection string will supply one.

## Progress log

- **2026-08-30 (task 2.5):** Wired `BatchInputForm`'s submit to actually populate `ResultsTable`, still against fake data. `results` state now lives in `App.tsx` (lifted so both the form and the table can reach it); submitting parses the textarea into names and matches each against `fakeResults` with a case-insensitive substring check, falling back to a "pending, no data" placeholder for unmatched names. Along the way: hit and fixed a real TypeScript string-literal-widening error on the fallback object (fixed the same way `fakeResults.ts` types its entries — an explicit `: CompanyResult` annotation).
- **2026-08-30 (task 2.6):** Basic styling pass. Cleared the dead Vite scaffold CSS out of `App.css` and replaced it with real rules for the form (flexbox stacking), the results table (borders, `border-collapse`, row hover highlight), and a new `.company-detail` card style. User asked the agent to drive the CSS directly rather than derive each property ("CSS isn't programming") — agreed compromise: agent writes it, user pastes it in, still predicts/verifies the result rather than a blind handoff.
- **2026-08-30 (task 2.7):** Section 2 fully shipped via the real feature-branch → PR → merge workflow for the first time in this project. Decided to commit `.claude/` to the public repo rather than gitignore it (harmless lesson-runner config, not worth hiding). Committed all of tasks 2.1–2.6 as one commit ("Build static UI"), pushed `feature/ui` with `git push -u`, opened PR #1 via `gh pr create`, self-reviewed the diff, squash-merged on GitHub, and cleaned up both the remote and local `feature/ui` branches. Hit (and correctly diagnosed via `git log`/`git status`, not panic) a `git branch -d` warning caused by squash-merge changing commit ancestry — a real, common git gotcha, not an actual problem.
- **2026-08-31 (task 3.1):** Started Section 3 (local backend server). Broke it into 6 tasks in `plan.md`. Scaffolded `backend/` as its own npm project, sibling to `frontend/`: `npm init -y`, installed `express` as a runtime dependency and `typescript`/`@types/node`/`@types/express`/`tsx` as dev dependencies, generated `tsconfig.json` via `npx tsc --init` and hand-edited `types: []` to `types: ["node"]`, and added a `dev` script (`tsx watch server.ts`) that will work once `server.ts` exists next lesson. Caught and fixed a real gap before it became a problem: `backend/` had no `.gitignore`, so `node_modules` was about to be committable; user predicted this correctly when asked, then added `backend/.gitignore` and verified the fix with `git status --untracked-files=all`. No server code written yet — that's task 3.2.
- **2026-09-03 (tasks 3.2–3.4):** Wrote the first real running server in this project. User wrote all of `server.ts` unassisted after each piece (app instance, route, listen) was explained separately — correct on the first pass. Ran it, predicted and confirmed the startup log, then predicted and confirmed the browser response at `/health`. Added CORS: user's first two predictions about what breaks without it were both wrong (guessed a 404, then guessed the browser blocks the request pre-send) — corrected both times, real gap, not yet independently restated correctly, worth a review pass later in Section 4 when the frontend actually calls this endpoint. Installed `cors` + `@types/cors`, added `app.use(cors())`. Side quest: user independently changed `backend/package.json`'s `"type"` from `"commonjs"` to `"module"` on their own initiative (reasoning was partially off — corrected); confirmed the server still ran fine afterward. Stopped before task 3.5 (wiring the frontend to call `/health`) since it's a new kind of work — the first `useEffect` — and deserved its own session rather than being rushed in.
- **2026-09-03 (task 3.5, same day):** Before continuing, created a feature branch (`backend-setup`, user's own name choice) for all of Section 3's uncommitted work — correcting a process gap, since the git workflow calls for a branch starting Section 2 and the prior session had (wrongly) suggested committing tasks 3.2–3.4 straight to `main`. Then wired the frontend to `/health`: taught `useEffect`, Promises/`async`/`await`, and `fetch` together. User wrote the state, the async fetch/parse logic, and the conditional render themselves; initially omitted the `useEffect` dependency array (a live instance of a misconception just corrected moments earlier in a prediction question) but self-caught and fixed it unassisted. Confirmed working in the browser: the real string `"ok"`, fetched live from the Express server, rendered on the page — Section 3's deliverable (a real frontend-to-backend connection) reached. Following the section-2 precedent, held off on committing until the whole section (task 3.6: commit + push + PR + merge) is ready to go as one unit.
- **2026-09-03 (task 3.6, Section 3 complete):** Ran the full commit → push → PR → merge → cleanup cycle on `backend-setup`, notably more independently than task 2.7 — staged, committed ("Create backend server"), pushed with `-u`, opened and merged PR #2, and deleted both branches largely unprompted. Correctly predicted that `git add .` run from inside `backend/` (vs. the repo root) would only stage backend files, and correctly explained the `-u`/tracking mechanic that lets a later plain `git push` work — a gap explicitly left open after task 2.7, now closed. Skipped the pre-merge diff review in the moment; did it retrospectively instead (`git show a5cd8fd --stat`), correctly describing the new `backend/` folder and `App.tsx` change, though missed that the `learning/` docs rode along in the same commit. Confirmed `backend/node_modules` never appeared in the merged diff — task 3.1's `.gitignore` fix holding up in practice. Section 3 (a simple local backend server) is now fully done: a real Express server, reachable from the React frontend, shipped through the real GitHub workflow.
- **2026-09-04 (task 4.1):** Started Section 4 (real API endpoints, no persistence yet), broken into 6 tasks in `plan.md`. In `backend/server.ts`, added a `CompanyResult` interface mirroring the frontend's shape, a `BatchRun` interface wrapping it (`id`, `status`, `results: CompanyResult[]`), and the in-memory store itself (`const batchRuns: BatchRun[] = []`) — mirrors the persistent-vs-in-memory distinction already understood from the Postgres decision. Real discovery along the way: user placed the store's declaration *above* the `BatchRun` interface it references, correctly predicted TypeScript wouldn't mind, and confirmed via `npx tsc --noEmit` — first real contact with the type-level-vs-value-level declaration-order distinction. Created a new feature branch, `batch-endpoints`, for the rest of Section 4's work (proactively, this time, rather than after the fact like Section 3).
- **2026-09-05 (task 4.2):** Wrote `POST /batches` in `backend/server.ts`. Added `app.use(express.json())` (built-in body-parsing middleware, same `app.use` pattern as `cors()`) so `req.body` is populated. User built the handler mostly independently: destructuring `companyNames` off `req.body` needed one hint (first attempt grabbed the whole body, not the field — corrected via the already-known component-props destructuring pattern); introduced `crypto.randomUUID()` for generating ids, which user first called as `crypto.randomUUID()` despite importing the function directly (fixed to `randomUUID()`). Real bug surfaced and self-diagnosed in part: the `.map()` callback used a `{ }` block body to build each placeholder `CompanyResult` but never returned it — user's first prediction of the output was incomplete ("nothing") until the array-of-`undefined` mechanism was explained, after which they correctly supplied the `return temp;` fix. Also caught, after being asked what the frontend would actually receive, that the handler was sending back a hardcoded `{ status: "ok" }` instead of the real `batch` — fixed to `res.json(batch)`. Renamed `foundCompanies`/`temp` to `results`/`result` (agent-suggested names, user applied them) to set up object-shorthand (`{ results }`) in the `BatchRun` construction. Review question at the top of the lesson (forward-looking, no code in front of them) confirmed `Modules / imports & exports` as solid — bumped to `understood`. Held off on committing, per the Section 3 precedent — the whole of Section 4 goes up as one PR once task 4.6 is reached.
- **2026-09-05 (task 4.3, same day):** Wrote `GET /batches/:id` in `backend/server.ts`. User registered the route, but first without the `:id` route-parameter placeholder (`"/batches/"`) — self-corrected to `"/batches/:id"` once asked where the placeholder needed to go, then independently and correctly destructured `const { id } = req.params;` on the first try (no hint needed, unlike the `req.body` destructuring last lesson — real progress). Then hit the *same* block-body-missing-return bug as task 4.2's `.map()`, this time in a `.find()` callback — first fix attempt added `return batch;` (always truthy, so `.find()` would always return the first array element regardless of the requested id); correctly predicted this exact failure mode unprompted when asked, then independently rewrote it as `return batch.id === id;`. This second clean encounter with the return-statement mechanism, plus the independent `req.params` destructuring, was enough to bump both `Object destructuring` and `Arrow function block bodies & explicit return` to `understood`. Also introduced and used correctly on the first attempt: a real `if`/`else` statement (this project's first, replacing the `&&`/ternary-only conditionals used so far) and `res.status(404)` for the not-found case. Held off on committing, same as 4.2 — Section 4 goes up as one PR at task 4.6.
- **2026-09-05 (task 4.4, same day):** Manually tested both routes in Postman before touching the frontend, per the plan. Started the backend server; sent `POST /batches` with `{ "companyNames": ["Acme", "Globex"] }` and got back a `200` with the full `BatchRun` (correctly predicted the general shape beforehand, though didn't initially mention the outer `BatchRun` id distinct from the per-company ids — pointed out afterward since it's the id `GET /batches/:id` actually needs). Used that outer id to test `GET /batches/:id`: correctly predicted and confirmed the same `BatchRun` came back. Tested the not-found branch with a bogus id: correctly predicted `404`, confirmed body `{ "error": "not found" }`. Both routes now verified working end to end outside the app, closing out Section 4's manual-testing checkpoint before wiring the real frontend in task 4.5. Side quest right after: tried retesting `GET /batches/:id` via Postman's Params tab (query string, `?id=...`) instead of the path, hit an HTML 404 instead of the expected JSON. Worked through it via questions rather than a direct answer — correctly identified the URL had nothing after `/batches/` before the `?`, correctly reasoned that doesn't match `/batches/:id`, and correctly read the HTML-vs-JSON 404 body as proof the request never reached the route handler at all. Real, useful distinction (route params vs. query strings) surfaced by the learner's own exploration, not scripted into the task.
- **2026-09-05 (task 4.5, Section 4 deliverable reached):** Rewrote `handleSubmit` in `App.tsx` to talk to the real backend instead of `fakeResults`. First half: an `async` `POST` to `/batches` sending `{ companyNames }` as a JSON body. User correctly described the plan (method/headers/body) before writing any code, and added `try`/`catch` plus a `response.ok` check on their own initiative — unprompted, not yet formally taught, and used correctly. Hit a real bug: the first attempt sent `JSON.stringify(companyNames)` (the bare array) instead of wrapping it in an object, which didn't match the backend's `const { companyNames } = req.body` destructuring; worked through it via questions rather than being told, landing on the actual mechanism (`req.body` would be the array itself, so destructuring `companyNames` off it yields `undefined`, and `undefined.map()` throws). Along the way, hit the same "predicted 'nothing' instead of 'the value `undefined`'" misconception already seen in task 4.2 — recurring enough to get its own tracked knowledge-graph entry rather than staying folded into destructuring. Second half: added a chained `GET /batches/:id` using the `POST` response's `id`, correctly reasoned through needing `.results` off the fetched batch (not the whole object) before writing it, and self-caught nothing further except a small copy-pasted `response.status` instead of `fetchBatch.status` in the second error branch. Fully tested end-to-end in the browser: submitted "Acme, Globex, Initech," correctly predicted (then confirmed) three pending rows with empty briefs, and confirmed the detail view still works by clicking a row. Section 4's real deliverable is now live — submitting a batch through the actual UI hits the real Express server and displays a real response, no `fakeResults` involved. Only task 4.6 (commit + push + PR + merge) remains for Section 4, held until then per the established per-section-PR pattern.
- **2026-09-05 (side quest, same day):** User asked what files could be deleted now that `handleSubmit` no longer uses `fakeResults`. Checked with `grep` rather than guessing: `fakeResults.ts` itself is still needed (all three components import the `CompanyResult` type from it), but two dead imports had been left behind — the unused `fakeResults` value import in `App.tsx`, and a never-called `import { error } from "console"` in `server.ts` (a leftover from an earlier attempt). User removed both themselves; confirmed clean via `grep` afterward.
- **2026-09-05 (task 4.6, Section 4 fully complete):** User ran the commit → push → PR → merge cycle for Section 4 on their own, outside of a `/next-lesson` session — squash-merged as PR #3 ("Batch endpoints", commit `8026418`), containing exactly the 4.1–4.5 work. Discovered and reconciled at the start of the next lesson: `plan.md` still showed 4.6 unchecked even though `git log` proved the real workflow had already happened. Confirmed with the user before touching anything, then ticked the box rather than redoing the work. Section 4 (real API endpoints, no persistence) is now fully done and merged to `main`.
- **2026-09-06 (task 5.2):** Designed and created the actual schema in a new `backend/db/schema.sql`: `batch_runs` (`id` uuid PK, `status` text restricted via `CHECK` to `'pending'`/`'done'`) and `company_results` (`id` uuid PK, `batch_run_id` uuid foreign key referencing `batch_runs(id)`, `company_name`, a 3-value `status` `CHECK`, `brief`, `email_draft`) — mirroring the `BatchRun`/`CompanyResult` interfaces already in `server.ts`. First real SQL DDL in this project, and it showed: the first table's `CHECK` constraint took several wrong attempts (MySQL-style `enum()`, then `'pending' | 'done'`, then `'pending' OR 'done'` — none referencing the column) before landing on `status IN (...)` via a worked analogy; the foreign key also needed a correction (a malformed table-level `FOREIGN KEY (...)` clause simplified to the inline `REFERENCES batch_runs(id)` form). Real payoff: both the `CHECK` and the column/type structure transferred correctly, unassisted, to the second table on the first attempt. User correctly predicted `IF NOT EXISTS` was needed before re-running the file against an already-existing `batch_runs` table — an unprompted, independently proposed fix, not one that was suggested first. Also correctly predicted the `CHECK` constraint would show in its own separate `\d` section rather than inline on the column. Ran `psql -d lead_scout -f backend/db/schema.sql` and verified both tables via `psql \d`, confirming correct columns, both check constraints, and the foreign-key relationship. No commit yet — held for the full Section 5 PR, same pattern as Sections 3 and 4.
- **2026-09-07 (task 5.5):** Rewrote `GET /batches/:id` in `backend/server.ts` to read from Postgres instead of the in-memory array, then deleted that array entirely. Real bugs surfaced and fixed in sequence, mirroring 5.4's pattern of learning from genuine errors: (1) the same truthy/falsy `if (result) {...} else {404}` dead-code shape from task 5.3 recurred on the first attempt — this time needing a direct nudge back to yesterday's fix rather than being self-caught, corrected to `result.rows.length > 0`. (2) `res.json(result)` sent the whole `QueryResult` object instead of the actual row; walked through `result.rows` → `result.rows[0]` (since `id` is a `PRIMARY KEY`, at most one match) via two rounds of guiding questions. (3) The big one: `pool.query<CompanyResult>(...)` falsely typed raw rows as already camelCase, when Postgres actually returns the real snake_case column names (`company_name`, `email_draft`) — confirmed via a real request showing the true JSON shape, contradicting an initial wrong prediction that it'd already be `companyName`. Correctly self-diagnosed the mechanism unprompted ("CompanyResult doesn't have company_name but that is what is returned by the query"), removed the inaccurate generic, and wrote a `.map()` to translate each raw row into a proper camelCase `CompanyResult` — independently choosing to destructure into a merged object for the response. (4) A genuinely new TypeScript nuance: `result.rows[0]` typed as `BatchRun | undefined` even after an earlier `.length` check, since narrowing only tracks the exact expression checked, not a related one — confirmed via real `npx tsc --noEmit` output, fixed by checking the actual `result.rows[0]` value (stored in a variable) directly. Also asked, unprompted, whether the same generic-imprecision issue applied to `pool.query<BatchRun>(...)` too (it does, but harmlessly, since only the genuinely-present `status` field is read) — real transfer of the pattern to a second query. Finished by deleting the now-dead `batchRuns` array and its `.push()` (deliberately left in place since 5.4, with the user's own `// delete after updating the GET request` marker) and an unused `type UUID` import. Confirmed clean via `npx tsc --noEmit` and a live request returning correctly-shaped `companyName`/`emailDraft` data. Only 5.6 (restart-survival retest) and 5.7 (commit/PR) remain for Section 5.
- **2026-09-07 (task 5.4):** Rewrote `POST /batches` in `backend/server.ts` to write real rows into Postgres instead of only the in-memory `batchRuns` array (which is deliberately kept for one more lesson, since `GET /batches/:id`, task 5.5, still reads from it). First taught parameterized queries (`$1`/`$2` placeholders instead of string-splicing values) as a defense against SQL injection; user applied the pattern correctly on the first attempt for the `batch_runs` insert. Real, valuable bugs surfaced and worked through rather than avoided: (1) adding `async` to a `.map()` callback silently changed `results`' type to an array of Promises rather than `CompanyResult[]` — user's first prediction of the resulting type was incomplete, landed on "array of promises" after one follow-up question, then correctly applied `Promise.all` to resolve it back to real values, and gave an accurate unprompted summary of the whole mechanism afterward. (2) A genuine placeholder-count mismatch in the `company_results` `INSERT` (6 columns, 5 `$` placeholders), self-fixed after being asked to compare the two lists. (3) The big one: a real Postgres foreign-key violation (`violates foreign key constraint "company_results_batch_run_id_fkey"`) from inserting `company_results` rows before the `batch_runs` parent row existed — user correctly read the error's `detail` field, identified the fix direction unprompted, and after one guiding question (does `batch_runs` need any of the `results` data, or just `id`/`status`?) independently restructured the handler to insert `batch_runs` first. Also independently caught and fixed a camelCase-vs-snake_case column name mismatch (`companyName`/`emailDraft` vs. schema's `company_name`/`email_draft`) while fixing the FK bug, though without stating the case-folding reasoning aloud — flagged in the knowledge graph as `introduced` rather than fully demonstrated. Tested for real: user ran the server themselves, hit the route, confirmed working via temporary console statements (removed after), and a `psql` check confirmed real linked rows in both `batch_runs` and `company_results` (matching `batch_run_id`s). Only `GET /batches/:id` (task 5.5) and the restart-survival retest (5.6) remain before Section 5's deliverable is reached.
- **2026-09-07 (task 5.3):** Connected Express to Postgres for real. Installed `pg` (+ `@types/pg`), created `backend/.env` (`DATABASE_URL=postgresql://localhost:5432/lead_scout`, matching the trust-auth local setup from 5.1) and added `.env` to `backend/.gitignore`. Wrote `backend/db/pool.ts`: loads `.env` via Node's built-in `process.loadEnvFile()` (no `dotenv` package needed on Node 22) before constructing and exporting a `pg` `Pool` configured with the connection string. Wired it into `server.ts` via `import { pool } from "./db/pool.js"` (first real contact with `"module": "nodenext"`'s runtime-extension import rule) and a `checkConnection()` function that runs `pool.query("SELECT NOW()")` in a `try`/`catch` on startup. Real bug caught and self-fixed: the first version paired the `try`/`catch` with a redundant, unreachable `if (response) {...} else {...}` — worked through why (a successful query result is always truthy; failure means a *rejected* promise, not a falsy resolved value) and simplified it away. Ran the server twice (before and after the simplification) and confirmed a real Postgres timestamp logged both times — Section 5's driver/connectivity piece is done, next up is swapping the actual routes (5.4/5.5) from the in-memory array to real `INSERT`/`SELECT` calls. Also, mid-lesson: a genuine wrong prediction surfaced that a gitignored file would still show up (flagged) in `git status` — corrected against real output; `.gitignore` downgraded back to `practicing` to reflect that gap honestly.
- **2026-09-08 (task 5.6, unplanned addition ahead of the plan):** Before continuing the plan, user brought their own idea: wrap `POST /batches`' inserts in a database transaction, so a failure partway through a batch doesn't leave a half-written row behind. Taught as its own mini-lesson: a wrong first prediction ("`pool.query()` calls would stay in one transaction because pool pools all the connections together") was corrected — `pool.query()` checks out and returns a *different* connection per call, so transaction state (tied to one Postgres session) doesn't survive across calls; the fix is one dedicated client via `pool.connect()`. Correctly predicted, unprompted, that concurrent `client.query()` calls via `Promise.all` on a single client would execute sequentially rather than error (`pg` queues them client-side) — landed that one cold. User then independently rewrote both insert helpers to accept a `PoolClient` and wired `BEGIN`/insert-loop/`COMMIT` with `ROLLBACK`+`release()` in `catch`/`finally`; self-caught a missing `await` on the `ROLLBACK` during review, though first misdiagnosed the consequence backward (thought it'd block `finally`, when the real risk is releasing the connection back to the pool *before* the rollback finishes). Verified atomicity with a real forced failure (a `null` company name tripping the `company_name NOT NULL` constraint mid-batch) and confirmed via `psql` row counts, unchanged before/after. Also removed the now-redundant `GET /health` route and its frontend `useEffect`/state (a wiring smoke test from task 3.5, superseded by the real routes) plus a resulting unused `useEffect` import — walked through, step by step, why the dead code would actually throw an unhandled promise rejection (HTML 404 body failing `.json()` parsing), not just silently be `undefined`.
- **2026-09-08 (task 5.7, Section 5 deliverable reached):** Manually verified restart-persistence in Postman: `POST /batches` with two companies (Anthropic, Microsoft), then killed the server with `Ctrl+C`, restarted it with `npm run dev`, and `GET` the same batch id — got back the identical batch, proving Postgres (not server memory) is now the real source of truth. User correctly predicted the outcome and the mechanism unprompted before running the test. Section 5 (database + persistence) is now fully done except for 5.8 (commit + push via the feature-branch → PR → merge workflow).
- **2026-09-06 (Section 5 started, task 5.1):** Broke Section 5 (database + persistence) into 7 tasks in `plan.md`. Chose to defer `users`/`companies`/`drafts` tables to Section 6 (auth) since nothing needs them yet — Section 5's schema will just be `batch_runs` + `company_results`, matching the current in-memory shape. User correctly restated pgvector's role unprompted at the top of the lesson (bumped to reviewed) when asked about using Supabase instead of local Postgres + Railway — discussed the tradeoff (real Postgres is fine to swap later since the connection is just a connection string, but Supabase's Auth/auto-API would undercut Section 6's hand-built auth lesson) and parked it as a Section 9 deployment decision to revisit, without changing the locked stack table. Task 5.1 itself (install Postgres, start it, create a database) turned into a real unplanned debugging session: `brew install postgresql@17` succeeded (slow, compiling `openssl@3` from source since Homebrew dropped bottle support for Intel Macs), but a completely unrelated, forgotten PostgreSQL 15 install from June — installed via the official PostgreSQL.org installer, running as its own system `LaunchDaemon` — was already squatting on port 5432. This caused the fresh Postgres 17 service to crash-loop on start and caused `createdb`'s very first attempt to hit the *old*, differently-configured server and fail with a misleading password-auth error. User confirmed the old install was unused, then traced the whole chain themselves command-by-command (env vars → `pg_hba.conf` → `lsof` → `ps aux` → the actual crash log, which named the real cause: "Address already in use") and ran `sudo launchctl unload` on the old install's `.plist`; Homebrew's auto-restart then picked up the freed port on its own. `createdb lead_scout` succeeded on retry (correctly recognized as a genuine "silence = success" case, a real correction to an earlier prediction of a confirmation message), and `psql -l` confirmed the new database exists alongside Postgres's default `postgres`/`template0` databases. No project files changed this session (pure local-machine setup) — nothing to commit yet; per the established pattern, the whole of Section 5 will go up as one PR once task 5.7 is reached.
